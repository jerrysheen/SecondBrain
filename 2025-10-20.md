
- 09:57
	ResourceManager把GPU部分的数据怎么删掉？
	目前资源部分都是没有gpu release的， ResouceManager怎么让GPU Release， 我可能可以设计一些显式的接口， 因为GPU资源不是根据引用关系自动消除的么，  以及FBOManager也没有这个接口 [pinned::true]
	 
- 10:11 
	除了std::unordered_map,你还知道什么map，怎么优化 unordered_map的内存排布
	 
- 11:53 
	# 句柄与安全（强/弱 & generation）
	
	- **强句柄**：持有者延长资源寿命（引用计数 +1）
	    
	- **弱句柄**：仅记录槽位/代数，访问前需要 `Lock()`/验证 generation
	    
	- **generation**：资源池重用槽位时避免悬垂
	    
	
	`template<class T> struct StrongHandle { uint32_t idx, gen; /* AddRef/Release */ };  template<class T> struct WeakHandle {     uint32_t idx, gen;     StrongHandle<T> Lock() const; // 校验 gen，失败返回空 };` 